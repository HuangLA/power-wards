# Power Wards · AI Agent 工作规范

本文件适用于本项目根目录及所有子目录中的 AI agent 工作。目标是让工作范围清晰、实现可验证、交付真实，避免擅自开发和依据过时产物推导需求。

## 1. 开始工作前

1. 阅读本文件及根目录 `REQUIREMENTS.md`，再检查本次任务涉及的文件。
2. 若目录使用 Git，先查看工作区状态和相关差异，识别用户已有修改；没有 Git 时不要假定可以回滚。
3. 判断本次任务属于讨论、文档、设计、实现、修复还是验证，明确本次授权范围。
4. 复杂任务给出简短实施步骤与验收方式，然后推进；简单修改直接完成。
5. 先查看实际代码、配置和素材，再判断技术栈、运行方式和实现状态，不根据文件名猜测功能已完成。

## 2. 需求依据与授权边界

- 最新明确的用户指示优先；稳定产品需求以根目录 `REQUIREMENTS.md` 为准。本文件约束工作方式，不另行定义产品需求。
- 用户新决定与文档冲突时，以新决定为准，在本次授权范围内更新相关文档，说明变更。
- 普通设计取舍自行作出合理决定；仅在缺少会影响行为、数据或范围的关键信息时集中提问，不反复请求已经给予的授权。
- 用户要求讨论、评估、写文档或做设计时，只处理对应产物，不擅自开始应用实现、安装依赖或启动服务。
- 用户明确要求实现或修复后，持续完成授权范围内的必要工作与验证，不只给计划或每一步都等待确认。
- 收到停止、暂停或范围收缩指令时，立即停止相关变更，报告已发生的修改。不得自行撤销先前变更。
- 不擅自部署、公开发布、创建账号、发送外部消息或执行破坏性清理。需执行这些动作时，先完成可审阅的准备工作，再获得必要授权。
- 默认由当前 agent 完成。仅在用户明确要求并行 agent，或适用指令明确要求时委派，避免多个 agent 同时修改同一文件。

## 3. 项目当前背景

- 已有真实地图 `Game_map_7.41.jpg`，原始尺寸为 8909×8424。保持原图完整；派生素材与原图分开存放。
- 用户已放弃 Figma 交付。以后使用已认可概念图与根目录需求文档，不再把 Figma 连接、额度、设计链接或插件运行作为开发前置条件。
- `design/reference/concept.png` 可作视觉参考，但概念图中的文字、数量和眼位均不能代替产品规则或真实攻略数据。
- 根目录早期 HTML/CSS/JS 文件是未验证骨架。不能据其行为反推需求，也不能因为有文件就声称应用可用。
- `design/` 中旧需求副本、Figma 状态、插件包和 ZIP 是历史产物，可能过时。无需自动重建、同步或清理；有冲突时遵循根目录需求与最新用户指示。
- 编写本文件不代表已获准开始应用实现。开始新任务时以该任务的具体授权为准。

## 4. 必须保留的产品约束

以下是容易在实现中出错的摘要；完整规则及待定事项见 `REQUIREMENTS.md`。

- 中文界面；React + TypeScript + Vite，共用核心配合 Electron 桌面壳。
- 首版本地使用，无账号、业务后端、云同步、在线分享链接或多人协作。
- 地图一次只显示一个 Profile。任意 Profile 可直接编辑、保存、另存为；导入的是独立本地副本。
- 明确保存与未保存状态。禁止用自动保存替代保存按钮；网页意外关闭不保存修改，不自动创建恢复草稿。
- 有未保存修改时，导出必须先成功保存或另存为；取消或失败则不导出。
- JSON 分享全部约定文字、说明、位置、颜色、分类、标签和必要元数据，不包含截图二进制或图片打包，不依赖发送者的本机图片路径。
- 同名自动添加后缀，重复导入创建独立 Profile。不同地图版本允许导入，但明确提示眼位可能偏移，不静默修正坐标。
- 四种阵营/用途组合独立多选，不把「天辉进攻 + 夜魇防守」误匹配为另外两种组合。
- 标签初始全选，部分选择时按任一所选标签匹配。未定的边界行为不能从早期实现中推导。
- 眼位存相对地图坐标；Ctrl + 左键新增、点击眼位选择并聚焦、滚轮缩放、左键拖动平移、右键恢复全图。拖动不得误触新增或选择。
- 支持重新定位、删除和撤销删除。撤销恢复关联资料，不能仅恢复一个空标记。
- 实战截图与攻略内容不得伪造。用于界面预览的地图裁切、虚构点位和说明必须明确标识为示例。

## 5. 实现方式

- 先完成范围明确、可运行的功能链路，再补充必要边界处理，避免未经要求的功能扩张和大范围重构。
- 界面、领域规则、地图坐标变换和存储访问保持清晰分离。网页 IndexedDB 与桌面应用数据目录通过存储接口适配，不复制两套业务逻辑。
- 桌面本地文件访问走受控接口，不向渲染层开放任意文件路径或任意命令执行能力。
- 导入内容视为不可信输入：校验格式版本、字段类型、坐标范围和合理资源限制，避免未经校验写入现有资料。
- 保存成功后才清除未保存状态；保存失败保留编辑内容并给出可理解的提示。避免不完整写入破坏上一次已保存版本。
- 修改眼位或撤销操作时保持位置、分类、文字和截图关联一致。
- 大图加载、切片层级和缩放性能用真实素材验证；没有测量就不声称性能问题已解决。
- 优先使用已有合适依赖；确需新增时说明用途，保持依赖精简。记录实际可用的启动和构建命令，不编造脚本。
- 产品界面使用用户理解的语言，不把内部数据库、组件架构或工具调用细节塞进用户操作流程。

## 6. 文件与变更管理

- 只修改本次任务必要的文件；保留用户已有工作和无关改动。
- 不随意删除原图、历史设计或用户资料，不用强制重置、覆盖整个目录等方式解决局部问题。
- 临时验证素材、构建产物与源码区分存放；不把截图中的本机路径、凭据或私密信息写入可分享资料。
- 新增说明保持简洁，注明实际状态。过时文档仅在任务需要时更新，避免产生多个互相矛盾的需求真源。
- 代码注释解释必要的原因、约束与不明显行为，不重复代码本身。

## 7. 验证标准

- 按改动风险选择验证。文档修改检查一致性与关键遗漏，不为纯文档增加测试。
- 领域逻辑优先验证：独立分类组合筛选、标签匹配、相对坐标换算、Profile 保存/另存为、导入冲突、导出前保存及撤销删除。
- 涉及持久化时验证保存、重新读取、失败保留编辑内容；涉及 JSON 时验证中文、换行、说明完整性和不含图片。
- 地图与交互改动须在实际页面检查缩放、拖动、点击区分、侧栏和浮层；只通过编译不能算交互验证。
- 桌面专属功能应在桌面环境验证。不能以网页通过代替 Electron 验收。
- 运行与改动相关的现有检查；检查失败先定位并修复。若受环境或权限阻碍，报告确切原因、已验证范围和未验证内容。
- 不写只重复实现细节的测试，不为可逆的小改动堆积测试。适当检查已通过后，除非出现新变更或疑点，不反复运行无关检查。
- 区分语法检查、模拟验证、实际运行和视觉检查，不将其中一种包装成全部验证完成。

## 8. 沟通与交付

- 开始时简要说明将做什么。持续工作中报告有意义的进展、发现与阻碍，不刷屏记录每个命令。
- 用户途中提出问题或修正时先响应并纳入当前任务；明确停止时立即停止。
- 工具失败后根据错误调整；权限、配额或外部服务阻断时如实报告，不伪造文件、链接、测试或完成状态，不绕过限制。
- 最终交付说明：完成了什么、关键文件、如何运行或查看、做过哪些验证，以及重要未完成项。
- 本地文件使用可点击的绝对路径；外部链接必须是实际创建或核实过的链接。
- 未实现、未运行或未验证的内容明确标识，不使用“已完成”掩盖部分交付。
- 不在没有必要时要求用户重复确认，也不把尚未授权的下一阶段自动启动。
