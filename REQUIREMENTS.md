# Power Wards：需求与技术设计

更新日期：2026-09-23

本文记录已确认的产品规则、技术路线、验收标准和当前实现状态。实现阶段不得从早期代码反推需求；后续改动以用户最新指示为准，代码及验证状态见第 11 节。

> 2026-09-23 首版已实现；产品规则、实现状态与验证结果见第 11 节。

## 1. 产品定位与范围

### 已确认

- 产品是 Dota 2 Observer Ward（侦察守卫）眼位编辑器，用于社区传播个人、朋友或职业选手分享的眼位资料。
- 项目已有完整地图 `Game_map_7.41.jpg`，尺寸为 **8909 × 8424**。
- 同一核心应用支持浏览器网页端和桌面客户端，采用中文界面。
- 首版本地使用，不包含账号、云业务后端、云同步、在线分享链接或多人协作；网页端使用本机服务写入本机文件。
- 网页与桌面各自保存本地资料，不自动同步；通过文件导入、导出交换 Profile。

### 当前实现状态

根目录中的 `index.html`、`style.css`、`data.js`、`package.json`、`server.js` 是早期未验证骨架，`map-preview.png` 是地图检查预览；它们不是当前应用入口，也不能作为需求依据。当前应用位于 `app/`，首版实现和实测状态见第 11 节。早期自动保存、截图随文件分享、标签一律 AND 等设想不替代本文确认的规则。

2026-09-23 决定：放弃 Figma 设计交付，后续以已认可概念图和本文为设计依据。已有 Figma 及本地设计包属于历史产物，不再作为开发前置条件。

## 2. 界面与信息组织

### 已确认

采用三栏布局：

| 区域 | 内容与行为 |
| --- | --- |
| 左侧 | 当前 Profile 切换与管理，以及分类、标签筛选 |
| 中心 | 大地图和可点击的眼位标记；筛选直接控制地图上眼位的可见性 |
| 右侧 | 仅选中具体眼位时出现可编辑详情；上方上传、显示本地游戏截图，下方编辑颜色、分类、自定义标签和详细文字说明 |

新增眼位时，底部弹出基础分类面板，快速设置阵营与进攻/防守用途。分类须以第 3 节的独立组合记录，不得因界面选择方式产生未选择的组合。

自定义标签用于整理与筛选，详细说明承载具体操作，例如：插眼前需砍树、插眼后砍哪棵树扩大视野、适用时机及其他特殊条件。

### 建议，尚非明确要求

- 两侧面板可收起；打开详情时避免地图布局跳动。
- 筛选使用正向显示选项，并显示结果数量、提供清除筛选入口。
- 布局与缩放切换应服务于查看目标及周边眼位。

## 3. 分类与筛选语义

### 已确认

一个眼位可以多重归类，分类是以下四种独立的阵营与用途组合，允许任意多选：

1. 天辉进攻
2. 天辉防守
3. 夜魇进攻
4. 夜魇防守

例如，眼位同时选择「天辉进攻」和「夜魇防守」，不自动获得「天辉防守」或「夜魇进攻」分类。数据应保留组合关系，不能只分别保存阵营集合与用途集合后计算笛卡尔积。

筛选阵营与用途时，两项条件必须在眼位的**同一个已选组合**中命中。因此上述眼位在「天辉 + 防守」筛选下不可见，在「天辉 + 进攻」或「夜魇 + 防守」下可见。

### 筛选定稿

同维度多选采用任一匹配，跨维度同时满足：至少一个已选分类组合的阵营和用途分别命中所选条件。阵营或用途任一组全不选时不显示眼位；全选表示该维度不额外缩小匹配范围。

### 标签筛选已确认

- 初始默认选中所有标签，不在自定义标签区显示“全部眼位”标签；点击某个标签后仅按该标签筛选，继续点击其他标签可多选。
- 按用户本次描述采用任一所选标签匹配（OR），不要求眼位同时拥有全部所选标签，不增加 AND/OR 模式切换。
- 默认全选表示不限制标签，初始展示全部符合分类条件的眼位；显式选择标签（即使选中了每个已有标签）仍排除无标签眼位。筛选区底部的“全选筛选项”按钮同时恢复阵营、用途和标签的默认全选状态。
- 标签全不选时不显示眼位。切换 Profile、新建或导入后保留阵营与用途选择，并重置标签筛选为默认全选。

## 4. Profile 与保存语义

### 已确认

- 地图每次仅显示一个当前 Profile，不能叠加多个 Profile 的眼位。
- 用户可新建自己的 Profile，或导入朋友、职业选手的 Profile。
- 任意 Profile 都可直接编辑、保存、另存为；无需先复制，不设只读限制。
- **保存**：将当前修改更新到当前 Profile。
- **另存为**：将当前内容保存为新 Profile，保留原 Profile 的已保存版本。
- **导入**：创建独立的本地 Profile 副本，不改发送者资料，避免无提示覆盖已有 Profile。
- 核心功能为新建、切换、导入、保存、另存为、导出。
- 修改后显示未保存标记；切换 Profile 或关闭时须提供离开保护，供用户保存、放弃修改或取消离开。不能用自动保存替代明确的保存语义。
- 不允许导出未保存修改：存在未保存修改时，必须先选择「保存」或「另存为」，成功后才可导出；取消或保存失败时不导出，也不悄悄改为导出旧版本。

### 平台行为

应用内切换、创建、导入及桌面关闭使用自定义「保存 / 放弃修改 / 取消」离开保护。

网页关闭或刷新受浏览器限制：使用浏览器原生未保存警告，不提供自定义三按钮关闭弹窗，也不在退出时异步保存。意外关闭默认丢弃未保存修改，不自动创建恢复草稿；重新打开时读取最近一次明确保存的版本。

网页 Profile 与截图保存在本机服务的数据目录，不依赖浏览器网站数据。清理浏览器数据不会删除已迁移的本机文件；仍未明确保存的编辑修改按既定规则丢弃。首次启动新网页存储时，如同一浏览器来源下存在旧版 IndexedDB 资料，则迁移 Profile 与截图至本机文件；校验成功后保留旧数据库，不删除来源资料。浏览器来源按协议、主机名和端口区分，迁移时应使用旧版应用相同的地址。

### Profile 管理约定

- 首版提供重命名和删除 Profile；新建、导入复用离开保护。
- 另存为后切换到新副本；同名追加数字后缀。保存失败保留编辑内容并提示。

## 5. 本地截图与 JSON 分享

### 已确认

首版通过 JSON 文件导入、导出 Profile。

| 分享文件应包含 | 分享文件不包含 |
| --- | --- |
| 全部文本信息，尤其是详细说明 | 图片二进制、Base64 图片或图片打包内容 |
| 眼位位置、颜色、分类、自定义标签 | 本机图片文件内容 |
| 必要 Profile 元数据、格式版本与地图版本 | 不要求携带本机图片路径 |

截图仅在当前设备本地保存，不随分享文件传播。接收者导入后自行上传自己的游戏截图。导入、导出必须完整保留约定分享的文字与眼位信息，不能因为不分享截图而遗漏说明。

- 同名或重复导入始终创建独立 Profile，同名自动添加后缀，不覆盖已有资料。
- 地图版本不同仍允许导入，明确提示「地图版本不同，眼位位置可能产生偏移」，不承诺位置准确，不自动迁移或修正坐标。

### 首版格式与资源约定

JSON 格式、字段与导入校验见第 11 节：首版使用格式版本 1，拒绝不支持的格式、不合法坐标、重复 id、超长字段或超限文件；异常以中文提示。
- 截图仅保存在本机，支持 PNG / JPEG / WebP，每张不超过 8 MiB、每眼位最多 10 张；已删除或移除的截图在保存成功后清理。
- 导入资料保留来源地图版本；不同版本允许导入并提示可能偏移，不静默修改坐标。

## 6. 地图交互与坐标

### 已确认

| 操作 | 行为 |
| --- | --- |
| Ctrl + 鼠标左键点击地图位置 | 新增眼位，并进入基础分类流程 |
| 左键点击已有眼位 | 选中并打开右侧详情，立即聚焦并放大眼位附近 |
| 地图右键 | 恢复全图视图 |
| 鼠标滚轮 | 缩放地图 |
| 左键按住拖动 | 平移地图 |

- 拖动不能误触点击、选择或新增；应正常区分点标记、背景拖动与 Ctrl 新增。
- 新增业务逻辑与触发手势解耦，未来可改为双击，但当前不启用双击新增。
- 眼位保存地图相对坐标，缩放、拖动及视口尺寸变化后仍对应原地图位置。
- 点击眼位聚焦目标为适应全图倍率的 5 倍，当前已经更近时不缩回；其余缩放边界与手势参数见第 11 节。
- 支持已有眼位重新定位、删除以及单级撤销；删除后 8 秒内可撤销，并恢复眼位及关联资料。

### 已采用的交互取舍

- 保留可见的添加按钮。
- 点击眼位采用固定目标缩放，当前已更近时不缩回。
- 右键恢复全图后保留当前选中眼位与详情。
- 滚轮缩放以指针为中心。

### 参数与已知限制

首版参数见第 11 节。聚焦动画、重叠标记散排和边界平移约束暂未实现；Ctrl 点击已有标记按选中处理。

## 7. 已认可的技术路线

- **React + TypeScript + Vite**：构建共用界面与业务逻辑。
- **Electron**：作为桌面客户端外壳。
- **网页存储**：浏览器页面通过版本化 HTTP API 访问运行在本机回环地址上的 Node 服务；服务将 Profile 与截图写入本机数据目录。此服务只服务本机网页，不包含账号或云同步。
- **桌面存储**：应用数据目录中的文件存储。
- 通过统一存储接口隔离平台差异，使 Profile 编辑、分类筛选、地图交互和 JSON 分享逻辑复用。
- 本地网页模式由 Node 服务同时提供前端文件和存储 API；开发模式的 Vite 页面通过仅允许本机来源的 CORS 访问回环地址 API。Electron 的本地文件访问由受控 IPC 提供。
- 当前没有云端服务。未来公开托管网页时，须提供独立云端 API、登录认证及服务端用户数据隔离；不能让云服务器直接写用户电脑，也不能将本机存储接口伪装成云同步。
- 网页与桌面存储互相独立，文件分享负责显式交换资料。

### 大图加载方案

对原始大图分层切片，按视野与缩放层级加载，眼位作为独立覆盖层渲染。相对坐标始终基于完整原地图，不随切片层级变化。

首版使用 `sharp` 生成五层 JPEG 切片，网页按视野与缩放层级加载，切片元数据及素材位于 `app/public/map/`。实测范围和未覆盖的性能情形见第 11 节；没有测量的设备与长会话性能不作保证。

### 接口与数据实现

存储抽象覆盖 Profile 列举、读取、明确保存、导入持久化以及本地截图读写。网页使用 `app/src/storage/localApi.ts` 访问 `app/local/server.mjs` 的版本化本机 API；Electron 使用受控 IPC 与应用数据目录。前端只依赖 `StorageAdapter`，未来云端模式可替换适配器并由 API 服务端加入认证和用户隔离。旧 `app/src/storage/webStorage.ts` 只用于读取并迁移旧版 IndexedDB，不作为当前网页存储。领域类型与 JSON 编解码分别见 `app/src/domain/types.ts`、`app/src/domain/share.ts`。

## 8. 首版实施路径

以下步骤用于记录首版实施路径，不是当前待执行计划；完成情况与未覆盖范围见第 11 节。

1. **规格与接口设计**：依据已确认的标签、导入、导出和保存规则，定义数据格式、存储接口、截图约束及剩余交互细节。
2. **共用核心与地图原型**：建立 React / TypeScript / Vite 结构；评估大图加载或切片方案；验证相对坐标、缩放平移、选择聚焦与 Ctrl 新增。
3. **Profile 与编辑流程**：实现单 Profile 展示、分类面板、详情编辑、本地截图、正确筛选及明确保存/另存为；网页和桌面分别接入本机文件服务与 Electron 应用数据目录。
4. **文件交换与数据保护**：实现不带截图的 JSON 导入导出、校验与独立副本创建，完成未保存提示和平台允许的离开保护。
5. **桌面适配**：接入 Electron 与应用数据目录存储，复用核心逻辑，验证桌面关闭保护及与网页的文件互通。
6. **验证与交付**：执行核心验收，评估大图性能和边界交互；提供本机网页服务、桌面构建及本地使用说明。公开网页和云端账号属于后续独立阶段。性能结论以实测为准。

## 9. 核心验收标准

| 编号 | 可验证条目 |
| --- | --- |
| A01 单 Profile | 准备两个含不同眼位的 Profile，切换后地图仅显示当前 Profile 符合筛选的眼位，不叠加前一个 Profile。 |
| A02 任意 Profile 可编辑 | 新建及导入的 Profile 均可直接修改颜色、分类、标签、说明和本地截图，无需先复制；保存后重新读取仍保留修改。 |
| A03 保存与另存为 | 保存更新当前 Profile；另存为创建新 Profile，原 Profile 的已保存内容不被覆盖。 |
| A04 组合筛选 | 对仅含「天辉进攻 + 夜魇防守」的眼位，天辉进攻、夜魇防守条件分别命中，天辉防守、夜魇进攻条件均不命中；覆盖四种分类及任意多选，不产生交叉误匹配。阵营或用途全不选时结果为空。 |
| A05 坐标稳定 | 在地图不同位置新增眼位，预览和正式眼位图标的中心均与点击坐标对齐；多次缩放、拖动、聚焦、恢复全图及调整视口后，标记仍落在同一地图地物上；保存后重载、导出再导入亦保持位置。 |
| A06 手势区分 | Ctrl 点击地图新增一次并出现底部分类面板；点击已有眼位打开详情并聚焦；拖动不新增、不误选；滚轮缩放与右键恢复全图正常；双击不触发专门的新增流程。 |
| A07 详情与本地截图 | 未选中眼位时不显示右侧详情；选中后上方显示截图、下方可编辑各字段；本地保存并重启后截图仍可读取。 |
| A08 JSON 完整与无图片 | 用包含中文、换行、特殊字符说明、多分类及多标签的 Profile 导出并在另一端导入，所有分享文本、位置、颜色、分类、标签及必要元数据按格式约定还原；检查文件不含图片内容或打包图片，不依赖发送者本机图片路径。接收端自行上传截图。 |
| A09 导入独立性 | 导入创建独立 Profile；同名自动加后缀，重复导入不覆盖已有资料。异地图版本允许导入，明确提示眼位可能偏移。本地编辑不影响来源 Profile 或发送者资料。 |
| A10 未保存提示 | 修改显示未保存标记；应用内切换时保存可持久化后切换，放弃恢复已保存内容后切换，取消则保留当前编辑状态。桌面关闭按同样语义验证；网页正常关闭采用浏览器允许的警告，意外关闭不保存未保存修改，重开读取最后明确保存版本，无自动恢复草稿。 |
| A11 跨端复用 | 网页与 Electron 使用同一界面和核心业务逻辑，通过存储适配分别由本机 HTTP 服务和 Electron IPC 写入本机文件；两端导出 JSON 可互相导入，且本地数据不会自动同步。 |
| A12 大图验证 | 使用真实 8909 × 8424 地图，记录首次加载、缩放、平移及眼位叠加表现；若采用切片，验证视野/层级加载和相对坐标对齐。具体性能阈值待评估后制定，不能提前声称通过。 |
| A13 标签筛选 | 初始全选不限制标签；点击任一标签后仅按该标签筛选，继续点击可多选，按任一所选标签匹配；显式选择标签排除无标签眼位，全不选时结果为空；筛选区底部按钮恢复全选。 |
| A14 导出前保存 | 有未保存修改时导出必须先保存或另存为；成功后导出该保存结果；取消或保存失败不得生成导出文件。 |
| A15 重新定位与撤销删除 | 重新定位只更新位置并标记未保存；删除后眼位不显示，撤销删除完整恢复位置、颜色、分类、标签、说明及本地截图关联。 |

## 10. 首版决策状态

此前待确认的筛选语义、地图交互参数、保存流程、JSON 格式、截图限制和 Profile 管理方式均已在首版中定稿，具体规则见第 11 节。后续变更应按用户最新指示更新产品规则和实现状态。

## 11. 实现定稿与验证结果（2026-09-23 首版实现）

当前实现位于 `app/`（React + TypeScript + Vite + Electron），根目录早期骨架与原图保持不动。本节记录已确认的首版行为与验证范围；后续若修改，应同步更新。

### 定稿决策

- **筛选语义**：同维度多选任一命中，跨维度须在眼位同一个已选组合中同时命中；阵营或用途任一组全不选时结果为空。标签初始全选且不限制标签，包含无标签眼位；点击标签进入按标签筛选，可多选且任一命中（OR），显式选中所有已有标签也不包含无标签眼位；标签全不选时结果为空。筛选区底部按钮恢复阵营、用途和标签全选；切换 Profile / 新建 / 导入后保留阵营与用途选择，标签重置为默认全选。
- **地图手势与参数**：Ctrl + 左键点击空白处新增；Ctrl + 点击已有标记按选中处理，不新增；双击无专门行为。点击/拖动阈值为 4px；滚轮缩放以指针为中心，步长 `1.0016^(-deltaY)`；缩放范围 `[适应缩放 × 0.9, 2.5]`；点击眼位聚焦目标倍率为适应缩放 × 5，当前已更近时不缩回；右键恢复全图并保留选中眼位。
- **保存与导出**：另存为后当前 Profile 切换到新副本；同名一律追加 ` (2)`、` (3)` 后缀；保存失败保留编辑内容与未保存标记并提示。保存期间继续产生的修改会保留为未保存状态；若另存为进行时发生新编辑，副本按操作开始时的内容创建，新修改仍留在原 Profile。导出前守卫提供「保存并导出 / 另存为 / 取消」，若保存期间有新修改则阻止本次导出。
- **JSON 格式**：`format = "power-wards-profile"`，`formatVersion = 1`，含 `mapVersion`、`exportedAt`、`profile.name`、`profile.wards[]`（id、x、y、name、color、categories、tags、description、createdAt、updatedAt）。不含任何截图字段、图片二进制或本机路径；导入时忽略并丢弃混入的此类字段。UTF-8 文件大小超过 10 MiB、格式版本不符、坐标越界、id 重复、字段超长或数量超限（>2000 眼位）均拒绝导入并给出中文原因；地图版本不同允许导入并提示眼位可能偏移，来源版本号保留在 Profile 元数据中。
- **截图**：PNG / JPEG / WebP，每张 ≤ 8 MB，每个眼位 ≤ 10 张，仅存本机（网页服务数据目录 `screenshots/` / 桌面 `userData/power-wards/screenshots/`）。删除眼位不立即删截图，保存成功时清理无引用的截图；显式移除的截图在保存时清理。
- **撤销删除**：单级撤销，入口为删除后 8 秒提示条；恢复完整眼位资料与截图关联。新增眼位或保存后旧撤销记录失效。
- **采纳的建议**：可见「＋ 添加眼位」按钮（点击进入放置模式，Esc 取消）、重命名 / 删除 Profile、重置筛选与结果计数。可收起侧栏首版未采纳。
- **网页离开保护**：有未保存修改时通过 `beforeunload` 触发浏览器原生离开警告；意外关闭不保存修改，无恢复草稿。桌面关闭由主进程拦截并复用应用内「保存 / 放弃修改 / 取消」三选。

### 验证结果（本机实测）

- Vitest 单元测试 64 项通过（`npm test`）：组合筛选穷举与任一空维度无结果（A04）、标签 OR 匹配与全不选无结果（A13）、坐标换算（A05 数学层）、保存 / 另存为并发编辑（A03）、导出守卫（A14）、导入独立性与字节数限制（A09）、撤销删除（A15）、Profile 删除与进行中的保存（A10）、分享 JSON 往返与校验（A08）、本机服务断线与重连提示。本机服务 Node 集成测试 3 项同由 `npm test` 执行，覆盖普通文件写入/读取、原子更新和删除、路径/字段/来源校验。
- 网页端真实浏览器验证 61 项通过（`npm run verify:web`，Chromium）：含旧 IndexedDB 首次迁移、清除浏览器数据库后从本机文件重载，以及 Profile JSON 与截图确实落盘；另覆盖地图切片、眼位预览和正式标记的中心对齐、缩放平移交互、分类全不选、标签全不选、不同标签多选与单一标签排除无标签眼位、截图刷新、导入导出、保存守卫、撤销和无页面运行错误。
- 桌面端验证 10 项通过（`npm run verify:electron`，真实 Electron；源码运行与 Windows 便携版均已验证）：应用协议加载切片、缓存与 Profile 分离、受控桥接存储、Profile 与截图文件落盘、关闭拦截三选并保存、重启读取（A10 桌面部分、A11 存储适配）。
- A12 实测记录：本地静态服务下首屏可见区域（适应缩放，层级 1）约 130–180ms 加载 9 片；切片金字塔共 5 层 425 片约 15.8 MB；缩放时按视野加载对应层级。未做长会话帧率与弱机测量，不声明性能目标已达标。
- 网页端 `beforeunload` 依赖浏览器原生警告，无法自定义三选弹窗（平台限制，符合预期设计）。
- 网页本机服务与页面默认在 `localhost:5173` 同源运行并仅监听本机回环地址；不能把当前服务直接作为公开云端后端使用。公开托管需另建认证、用户隔离和云端存储实现。

### 首版未覆盖项

- Windows x64 便携 ZIP 可由 `npm run package:win` 打包，尚未提供安装器、数字签名或 macOS/Linux 安装包。
- 网页与桌面之间的 JSON 互通依赖双方各自验证通过的同一编解码实现，未做跨端真机互导测试。
- Electron 真实交互验证在 Windows 环境执行；macOS 关闭窗口后从 Dock 重开再关闭的生命周期未在 macOS 机器上实测。
- 超过数百眼位的大 Profile 未实测。
- 聚焦动画、重叠标记散排、边界平移约束为首版简化行为。
